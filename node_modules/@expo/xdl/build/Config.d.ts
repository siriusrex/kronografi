import { GetConfigOptions, ProjectConfig } from '@expo/config';
/**
 * Get the project config with mode defaulting to the ProjectSettings module.
 *
 * @param projectRoot
 * @param options if `mode` is not supplied it will be inferred from the ProjectSettings module.
 */
export declare function getProjectConfigAsync(projectRoot: string, options: Partial<GetConfigOptions>): Promise<ProjectConfig>;
interface ApiConfig {
    scheme: string;
    host: string;
    port: number | null;
}
interface XDLConfig {
    turtleApi: ApiConfig;
    api: ApiConfig;
    ngrok: {
        authToken: string;
        authTokenPublicId: string;
        domain: string;
    };
    developerTool: string | null;
    validation: {
        reactNativeVersionWarnings: boolean;
    };
    helpUrl: string;
    offline: boolean;
}
declare const config: XDLConfig;
export default config;
